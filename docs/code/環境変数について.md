## 原則
- frontアプリケーションで環境変数を使う場合には、`front/src/app/env.ts`から参照する。
- エディタの機能を用いて、環境変数への参照を把握できるようになります。

## 良い例
```ts:route.ts

import { GYAZO_DELETE_END_POINT, GYAZO_UPLOAD_END_POINT } from "@/app/env";

// (略)

export async function POST(req: Request) {
  try {
    const imageBuffer = await req.arrayBuffer();
    if (!imageBuffer) {
      return NextResponse.json(
        { error: '画像データが必要です' },
        { status: 400 }
      );
    }

    const url = `${GYAZO_UPLOAD_END_POINT}`; //

```

## ダメな例

```ts:route.ts

export async function POST(req: Request) {
  try {
    const imageBuffer = await req.arrayBuffer();
    if (!imageBuffer) {
      return NextResponse.json(
        { error: '画像データが必要です' },
        { status: 400 }
      );
    }

    const url = `${process.env.GYAZO_UPLOAD_END_POINT}`; //

```